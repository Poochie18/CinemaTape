# Чеклист перед запуском 📋

## ✅ Перевірка файлів

Переконайтесь, що всі файли створені:

### Конфігурація
- [x] `package.json` - залежності проєкту
- [x] `vite.config.js` - конфігурація Vite
- [x] `tailwind.config.js` - конфігурація Tailwind CSS
- [x] `postcss.config.js` - конфігурація PostCSS
- [x] `.gitignore` - ігнор файли для Git
- [x] `index.html` - головний HTML

### Вихідний код
- [x] `src/main.jsx` - точка входу
- [x] `src/App.jsx` - головний компонент
- [x] `src/index.css` - глобальні стилі
- [x] `src/db.js` - IndexedDB налаштування

### Сервіси
- [x] `src/services/tmdb.js` - TMDb API сервіс

### Компоненти
- [x] `src/components/Calendar.jsx`
- [x] `src/components/DayFilms.jsx`
- [x] `src/components/WatchLater.jsx`
- [x] `src/components/Statistics.jsx`
- [x] `src/components/AddMovieModal.jsx`
- [x] `src/components/MoveToWatchedModal.jsx`
- [x] `src/components/MovieSearch.jsx`
- [x] `src/components/StarRating.jsx`
- [x] `src/components/Modal.jsx`
- [x] `src/components/LoadingSpinner.jsx`
- [x] `src/components/EmptyState.jsx`

### Публічні файли
- [x] `public/film.svg` - іконка додатку

### Документація
- [x] `README.md` - головний readme
- [x] `QUICKSTART.md` - швидкий старт
- [x] `TIPS.md` - поради по використанню
- [x] `ARCHITECTURE.md` - архітектура проєкту

## 🚀 Крок за кроком

### 1. Встановлення залежностей
```bash
npm install
```

**Що має встановитись:**
- react + react-dom
- vite
- tailwindcss + autoprefixer + postcss
- dexie + dexie-react-hooks
- date-fns
- lucide-react
- react-hot-toast
- framer-motion

**Перевірка:** Відсутність помилок в консолі

### 2. Налаштування TMDb API ключа

**Файл:** `src/services/tmdb.js`

**Знайдіть рядок:**
```javascript
const TMDB_API_KEY = 'YOUR_TMDB_API_KEY_HERE';
```

**Замініть на:**
```javascript
const TMDB_API_KEY = 'ваш_реальний_ключ';
```

**Як отримати ключ:**
1. https://www.themoviedb.org/ → Зареєструватись
2. Settings → API → Request API Key
3. Заповнити форму (можна вказати personal/student project)
4. Скопіювати "API Key (v3 auth)"

### 3. Запуск dev сервера
```bash
npm run dev
```

**Очікуваний результат:**
```
VITE v5.0.8  ready in 500 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 4. Відкрити в браузері
**URL:** http://localhost:5173

**Що має бути видно:**
- ✅ Шапка з назвою "КіноЛітопис"
- ✅ Три вкладки: Календар | На потім | Статистика
- ✅ Календар з поточним місяцем
- ✅ Плаваюча кнопка "+" внизу справа
- ✅ Немає помилок в консолі браузера (F12)

### 5. Тест функціоналу

#### Тест 1: Пошук фільму
1. Натиснути "+" або вибрати день в календарі
2. Ввести в пошук "Matrix" (або будь-який фільм)
3. **Очікується:** Список фільмів з'являється через 1-2 секунди
4. **Якщо не працює:** Перевірте API ключ та інтернет

#### Тест 2: Додавання фільму
1. Вибрати фільм з результатів пошуку
2. Обрати дату, поставити оцінку, додати нотатку
3. Натиснути "Зберегти"
4. **Очікується:** Фільм з'являється в календарі на обраній даті
5. **Якщо не працює:** Перевірте консоль браузера (F12)

#### Тест 3: Список "На потім"
1. Перейти на вкладку "На потім"
2. Натиснути "Додати"
3. Знайти та вибрати фільм
4. **Очікується:** Фільм з'являється в списку
5. Натиснути "Переглянуто" на карточці
6. Вибрати дату та оцінку
7. **Очікується:** Фільм переміщається в календар

#### Тест 4: Статистика
1. Додати кілька фільмів
2. Перейти на вкладку "Статистика"
3. **Очікується:** Відображаються дані (кількість, графік)

#### Тест 5: Експорт/Імпорт
1. Натиснути "Експорт" вгорі
2. **Очікується:** Завантажується JSON файл
3. Натиснути "Імпорт"
4. Вибрати той самий файл
5. **Очікується:** Дані відновлюються

## 🐛 Можливі проблеми та рішення

### Помилка: "Cannot find module"
**Рішення:** 
```bash
rm -rf node_modules package-lock.json
npm install
```

### Помилка: "Failed to fetch" при пошуку
**Причини:**
1. Невірний API ключ → перевірте `src/services/tmdb.js`
2. Немає інтернету → перевірте з'єднання
3. TMDb недоступний → спробуйте пізніше

**Перевірка API ключа:**
```bash
curl "https://api.themoviedb.org/3/movie/550?api_key=ВАШ_КЛЮЧ"
```

### Порт 5173 зайнятий
**Рішення:** Vite автоматично знайде інший порт або:
```bash
npm run dev -- --port 3000
```

### Стилі не застосовуються
**Рішення:**
1. Перевірте, що Tailwind config правильний
2. Перезапустіть dev сервер
3. Очистіть кеш браузера (Ctrl+Shift+R)

### Дані не зберігаються
**Причини:**
1. Використовується режим інкогніто
2. Браузер блокує IndexedDB
3. Переповнено сховище браузера

**Рішення:**
- Використовуйте звичайний режим
- Перевірте налаштування браузера
- Очистіть старі дані

## 📱 Тестування на мобільних

### Chrome DevTools
1. F12 → Toggle device toolbar (Ctrl+Shift+M)
2. Вибрати пристрій (iPhone, iPad, Pixel)
3. Перевірити responsive дизайн

### Реальний пристрій
1. Знайти IP адресу комп'ютера:
```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```

2. Запустити Vite з --host:
```bash
npm run dev -- --host
```

3. Відкрити на телефоні: `http://ВАШ_IP:5173`

## 🎉 Готово!

Якщо всі тести пройшли успішно:
- ✅ Додаток працює
- ✅ Пошук знаходить фільми
- ✅ Дані зберігаються
- ✅ Експорт/імпорт функціонує
- ✅ Адаптивний дизайн працює

**Можна користуватись! 🎬✨**

---

## 🚢 Наступні кроки

### Production build
```bash
npm run build
```

Файли будуть в `dist/` - їх можна завантажити на хостинг.

### Рекомендовані хостинги (безкоштовні)
- **Vercel** - https://vercel.com (найпростіший)
- **Netlify** - https://netlify.com
- **GitHub Pages** - через Actions
- **Cloudflare Pages**

### Деплой на Vercel (найшвидший)
1. Встановити Vercel CLI: `npm i -g vercel`
2. В папці проєкту: `vercel`
3. Слідувати інструкціям
4. Готово! Отримаєте URL типу `https://kinolitopys.vercel.app`

---

**Приємного користування! Бажаю багато чудових фільмів! 🎬🍿**
